services:
  - type: web
    name: portfolio
    runtime: php
    buildCommand: composer install --no-dev
    startCommand: php -S 0.0.0.0:$PORT
    envVars:
      - key: PHP_VERSION
        value: "8.2"
